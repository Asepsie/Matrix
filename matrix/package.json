{
  "name": "project-matrix",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node --watch build.js",
    "test": "node --test tests/*.test.js",
    "smoke": "node tests/smoke.mjs",
    "verify": "node build.js && node --test tests/*.test.js && node tests/smoke.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.9.0"
  }
}
